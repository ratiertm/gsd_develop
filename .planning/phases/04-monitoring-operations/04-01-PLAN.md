---
phase: 04-monitoring-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kiwoom_trader/gui/__init__.py
  - kiwoom_trader/gui/main_window.py
  - kiwoom_trader/gui/widgets/__init__.py
  - kiwoom_trader/gui/widgets/toast_widget.py
  - kiwoom_trader/gui/notification/__init__.py
  - kiwoom_trader/gui/notification/notifier.py
  - kiwoom_trader/gui/notification/discord_sender.py
  - kiwoom_trader/config/settings.py
  - tests/test_notifier.py
  - tests/test_discord_sender.py
  - tests/test_toast_widget.py
autonomous: true
requirements:
  - NOTI-01
  - NOTI-02
  - NOTI-03

must_haves:
  truths:
    - "Notification dispatcher routes events to enabled channels (GUI toast, log, Discord)"
    - "Toast widget appears bottom-right and auto-dismisses after ~4 seconds"
    - "Discord webhook sends color-coded Embed (green=buy, red=sell) via background thread"
    - "Notification failures never block or crash the system"
    - "Each channel (GUI popup, log, Discord) can be independently toggled ON/OFF via config"
  artifacts:
    - path: "kiwoom_trader/gui/main_window.py"
      provides: "QMainWindow with QTabWidget skeleton (3 tabs) and show_toast method"
      min_lines: 50
    - path: "kiwoom_trader/gui/widgets/toast_widget.py"
      provides: "ToastWidget with fade animation and auto-dismiss"
      min_lines: 40
    - path: "kiwoom_trader/gui/notification/notifier.py"
      provides: "Central notification dispatcher"
      exports: ["Notifier"]
    - path: "kiwoom_trader/gui/notification/discord_sender.py"
      provides: "Discord webhook sender with QThread worker"
      exports: ["DiscordSendWorker", "build_trade_embed"]
  key_links:
    - from: "kiwoom_trader/gui/notification/notifier.py"
      to: "kiwoom_trader/gui/widgets/toast_widget.py"
      via: "main_window.show_toast() call"
      pattern: "show_toast"
    - from: "kiwoom_trader/gui/notification/notifier.py"
      to: "loguru"
      via: "logger.bind(log_type='trade')"
      pattern: "logger\\.bind.*trade"
    - from: "kiwoom_trader/gui/notification/discord_sender.py"
      to: "Discord webhook URL"
      via: "os.getenv('DISCORD_WEBHOOK_URL')"
      pattern: "DISCORD_WEBHOOK_URL"
---

<objective>
Build the GUI skeleton (MainWindow with 3 empty tabs) and the complete notification system (toast widget, log integration, Discord webhook).

Purpose: Establishes the GUI container that Plans 02-04 fill with content, and delivers the full notification pipeline that the dashboard and strategy engine will use to alert the user.

Output: MainWindow with tab structure, ToastWidget, Notifier dispatcher, DiscordSendWorker, notification config section in Settings, tests for all notification components.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-operations/04-CONTEXT.md
@.planning/phases/04-monitoring-operations/04-RESEARCH.md

@kiwoom_trader/config/settings.py
@kiwoom_trader/main.py
@kiwoom_trader/core/__init__.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From kiwoom_trader/config/settings.py:
```python
class Settings:
    def __init__(self, config_path: str = "config.json"):
    _config: dict  # Direct access to config dict
    _config_path: Path
    @staticmethod
    def _default_config() -> dict:  # Add notification section here
    @property
    def strategy_config(self) -> dict:
    @property
    def risk_config(self) -> RiskConfig:
```

From kiwoom_trader/utils/logger.py:
```python
# Existing loguru setup with trade/system/error sinks
# Use: logger.bind(log_type="trade").info("message") for trade log routing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GUI package scaffold + MainWindow + ToastWidget + notification config</name>
  <files>
    kiwoom_trader/gui/__init__.py,
    kiwoom_trader/gui/main_window.py,
    kiwoom_trader/gui/widgets/__init__.py,
    kiwoom_trader/gui/widgets/toast_widget.py,
    kiwoom_trader/gui/notification/__init__.py,
    kiwoom_trader/config/settings.py
  </files>
  <action>
1. Create `kiwoom_trader/gui/` package with `__init__.py` that exports MainWindow (use try/except PyQt5 fallback pattern like core/__init__.py).

2. Create `kiwoom_trader/gui/widgets/__init__.py` (empty).

3. Create `kiwoom_trader/gui/main_window.py`:
   - `MainWindow(QMainWindow)` with `__init__(self, settings: Settings)`.
   - QTabWidget with 3 tabs: "Dashboard" (QWidget placeholder), "Chart" (QWidget placeholder), "Strategy" (QWidget placeholder). Tabs replaced later by Plans 02-04.
   - setMinimumSize(1200, 800), setWindowTitle("KiwoomDayTrader").
   - `show_toast(self, title: str, message: str, event_type: str)` method that creates ToastWidget positioned bottom-right of the main window. Track active toasts in a list and offset Y position for stacking (Pitfall 5 from RESEARCH). Remove from list on dismiss.
   - Status bar at bottom showing "Ready" initially.

4. Create `kiwoom_trader/gui/widgets/toast_widget.py`:
   - `ToastWidget(QLabel)` per RESEARCH Pattern 4.
   - `__init__(self, parent, title, message, event_type, duration_ms=4000, on_dismiss=None)`.
   - Style by event_type: trade=green border, signal=blue border, error=red border. Dark background (#323232), white text.
   - QGraphicsOpacityEffect + QPropertyAnimation for fade-in (300ms) and fade-out (300ms).
   - QTimer auto-dismiss. Call on_dismiss callback when closing (for stack management).
   - setFixedWidth(320), adjustSize for height.

5. Create `kiwoom_trader/gui/notification/__init__.py` (empty).

6. Update `kiwoom_trader/config/settings.py`:
   - Add `notification` section to `_default_config()`:
     ```python
     "notification": {
         "gui_popup_enabled": True,
         "log_enabled": True,
         "discord_enabled": False,
         "discord_rate_limit_sec": 2,
     }
     ```
   - Add `save(self)` method that writes self._config to self._config_path with json.dump (indent=2, ensure_ascii=False). This is needed for GUI-03 hot-swap (Pitfall 3 from RESEARCH).
   - Add `@property notification_config(self) -> dict` returning self._config.get("notification", {}).
  </action>
  <verify>
    <automated>python -c "from kiwoom_trader.config.settings import Settings; s = Settings.__new__(Settings); s._config = Settings._default_config(); assert 'notification' in s._config; assert hasattr(Settings, 'save')"</automated>
  </verify>
  <done>GUI package exists with MainWindow (3 tabs, toast support), ToastWidget with fade animation, Settings has notification config section and save() method</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notifier dispatcher + Discord sender + tests</name>
  <files>
    kiwoom_trader/gui/notification/notifier.py,
    kiwoom_trader/gui/notification/discord_sender.py,
    tests/test_notifier.py,
    tests/test_discord_sender.py,
    tests/test_toast_widget.py
  </files>
  <behavior>
    - test_notifier_routes_to_log: notify() with log_enabled=True calls logger.bind(log_type="trade")
    - test_notifier_routes_to_gui: notify() with gui_popup_enabled=True calls main_window.show_toast()
    - test_notifier_routes_to_discord: notify() with discord_enabled=True calls _send_discord()
    - test_notifier_skips_disabled_channels: disabled channels are NOT called
    - test_notifier_handles_no_main_window: main_window=None does not crash when gui_popup_enabled
    - test_discord_embed_buy_green: build_trade_embed with side="BUY" returns color=0x26A69A
    - test_discord_embed_sell_red: build_trade_embed with side="SELL" returns color=0xEF5350
    - test_discord_embed_fields: embed contains code, price, qty, strategy, pnl_pct fields
    - test_discord_sender_handles_no_url: DiscordSendWorker with empty URL does nothing (no crash)
    - test_discord_sender_handles_network_error: urlopen failure logs error, does not raise
    - test_discord_rate_limit: Notifier respects discord_rate_limit_sec (drops messages within interval)
    - test_toast_widget_creation: ToastWidget can be instantiated (mocked QLabel)
  </behavior>
  <action>
1. Create `kiwoom_trader/gui/notification/discord_sender.py`:
   - `build_trade_embed(trade_data: dict, side: str) -> dict` per RESEARCH code example. Color: 0x26A69A for BUY, 0xEF5350 for SELL. Fields: code, price (formatted with comma), qty, strategy, pnl_pct.
   - `DiscordSendWorker(QThread)` per RESEARCH Pattern 3. Use try/except PyQt5 fallback (QThread or threading.Thread). __init__(payload, webhook_url). run() does urllib.request.Request POST with 5s timeout. Catches all exceptions, logs error via loguru.

2. Create `kiwoom_trader/gui/notification/notifier.py`:
   - `Notifier.__init__(self, config: dict, main_window=None)` -- config is the notification section dict.
   - `notify(self, event_type: str, title: str, message: str, data: dict = None)`:
     - If log_enabled: `logger.bind(log_type="trade").info(f"[{event_type.upper()}] {title}: {message}")`
     - If gui_popup_enabled and main_window: `self._main_window.show_toast(title, message, event_type)`
     - If discord_enabled: check rate limit (time.time() - last_send >= discord_rate_limit_sec), then create DiscordSendWorker with build_trade_embed payload if data has "side" key, else simple content message. Start worker. Keep reference to prevent GC.
   - Rate limiting: track `_last_discord_time` (float). Skip Discord send if within interval. Log skipped message at DEBUG level.

3. Create tests:
   - `tests/test_notifier.py`: Mock main_window, mock logger, mock DiscordSendWorker. Test routing to each channel, test disabled channels skipped, test rate limiting.
   - `tests/test_discord_sender.py`: Test build_trade_embed output structure and colors. Mock urllib.request.urlopen for error handling test.
   - `tests/test_toast_widget.py`: Use MagicMock for QLabel parent. Test that ToastWidget can be constructed without real Qt (mock PyQt5 imports if needed, or test only the data/logic parts).
  </action>
  <verify>
    <automated>pytest tests/test_notifier.py tests/test_discord_sender.py tests/test_toast_widget.py -x -q --tb=short</automated>
  </verify>
  <done>Notifier dispatches to 3 channels with independent toggling, Discord sends color-coded embeds via background thread with rate limiting, all notification failures are caught and logged, tests pass</done>
</task>

</tasks>

<verification>
- `pytest tests/test_notifier.py tests/test_discord_sender.py tests/test_toast_widget.py -x -v` all pass
- `python -c "from kiwoom_trader.gui import MainWindow"` imports without error (or graceful fallback)
- `python -c "from kiwoom_trader.gui.notification.notifier import Notifier"` imports
- `python -c "from kiwoom_trader.gui.notification.discord_sender import build_trade_embed, DiscordSendWorker"` imports
- Settings._default_config() contains "notification" key
</verification>

<success_criteria>
- MainWindow class exists with QTabWidget (3 tabs) and show_toast() method
- ToastWidget renders with fade animation and auto-dismiss
- Notifier routes notifications to 3 independent channels
- Discord embeds are properly color-coded (buy=green, sell=red)
- Rate limiting prevents Discord webhook flooding
- All notification code is fire-and-forget (no exception propagation)
- Settings has save() method and notification config section
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-operations/04-01-SUMMARY.md`
</output>
