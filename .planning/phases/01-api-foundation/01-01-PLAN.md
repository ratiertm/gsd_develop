---
phase: 01-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kiwoom_trader/__init__.py
  - kiwoom_trader/config/__init__.py
  - kiwoom_trader/config/settings.py
  - kiwoom_trader/config/constants.py
  - kiwoom_trader/api/__init__.py
  - kiwoom_trader/utils/__init__.py
  - kiwoom_trader/utils/logger.py
  - config.json
  - .env.example
  - .gitignore
  - requirements.txt
  - pytest.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - CONN-03

must_haves:
  truths:
    - "Application loads config.json settings with sensible defaults when file is missing"
    - "Environment variables from .env are accessible via Settings class"
    - "Loguru writes to three separate daily-rotated log files (system, trade, error)"
    - "FID constants and error codes are importable from config.constants"
    - "pytest runs with shared mock fixtures for KiwoomAPI and QAxWidget"
  artifacts:
    - path: "kiwoom_trader/config/settings.py"
      provides: "JSON + .env config loader"
      exports: ["Settings"]
    - path: "kiwoom_trader/config/constants.py"
      provides: "FID codes, screen numbers, login error codes"
      exports: ["FID", "SCREEN", "LOGIN_ERROR"]
    - path: "kiwoom_trader/utils/logger.py"
      provides: "Multi-sink loguru configuration"
      exports: ["setup_logging"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
      contains: "mock_kiwoom_api"
  key_links:
    - from: "kiwoom_trader/config/settings.py"
      to: "config.json"
      via: "json.load"
      pattern: "json\\.load"
    - from: "kiwoom_trader/config/settings.py"
      to: ".env"
      via: "python-dotenv load_dotenv"
      pattern: "load_dotenv"
---

<objective>
Project scaffolding, configuration, logging, constants, and test infrastructure for KiwoomDayTrader.

Purpose: Establish the project skeleton, config management, and test fixtures that all subsequent plans depend on. This is the foundation -- every other module imports from config/ and utils/.

Output: Runnable Python package with config loading, structured logging, constants, and pytest test infrastructure ready for TDD in Plans 02 and 03.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-foundation/01-RESEARCH.md
@.planning/phases/01-api-foundation/01-CONTEXT.md
@.planning/phases/01-api-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project structure, config loader, constants, and logging</name>
  <files>
    kiwoom_trader/__init__.py,
    kiwoom_trader/config/__init__.py,
    kiwoom_trader/config/settings.py,
    kiwoom_trader/config/constants.py,
    kiwoom_trader/api/__init__.py,
    kiwoom_trader/utils/__init__.py,
    kiwoom_trader/utils/logger.py,
    config.json,
    .env.example,
    .gitignore,
    requirements.txt
  </files>
  <behavior>
    - Settings() loads config.json and returns config values; missing file returns defaults
    - Settings._default_config() returns dict with tr_interval_ms=4000, heartbeat_interval_ms=30000, max_reconnect_retries=5, real_data_fids, watchlist, log_dir
    - Settings.account_password reads KIWOOM_ACCOUNT_PW from env
    - Settings.is_simulation reads KIWOOM_SIMULATION from env, defaults to True
    - FID.CURRENT_PRICE == 10, FID.VOLUME == 13, LOGIN_ERROR.SUCCESS == 0
    - setup_logging() creates log directory and configures 3 sinks (system, trade, error) + console
  </behavior>
  <action>
    Create the full project directory structure per RESEARCH.md Pattern (kiwoom_trader/ with config/, api/, utils/ subpackages).

    **config/settings.py**: Settings class that loads config.json via json.load, falls back to _default_config() if file missing. Uses python-dotenv load_dotenv() for .env. Properties: account_password (KIWOOM_ACCOUNT_PW), is_simulation (KIWOOM_SIMULATION, default "true"). Config dict keys: tr_interval_ms (4000), heartbeat_interval_ms (30000), max_reconnect_retries (5), real_data_fids (stock_execution and stock_orderbook FID strings), watchlist ([]), log_dir ("logs").

    **config/constants.py**: Three classes -- FID (real-time Field IDs per RESEARCH.md: CURRENT_PRICE=10, PREV_CLOSE_DIFF=11, DIFF_RATE=12, VOLUME=13, EXEC_TIME=20, EXEC_VOLUME=15, OPEN_PRICE=16, HIGH_PRICE=17, LOW_PRICE=18, ASK_PRICE_1=27, BID_PRICE_1=28, TRADE_AMOUNT=14, EXEC_STRENGTH=228, ASK_PRICES list, BID_PRICES list, MARKET_OP=215), SCREEN (LOGIN="0000", TR_BASE=1000, REAL_BASE=5000), LOGIN_ERROR (SUCCESS=0, PASSWORD_ERROR=-100, ACCOUNT_DIFF=-101, MONTHLY_FEE_UNPAID=-102, IP_RESTRICTED=-103, VERSION_MISMATCH=-104, AUTH_ERROR=-105, USER_LOCKED=-106).

    **utils/logger.py**: setup_logging(log_dir="logs") function. Remove default loguru handler. Add: (1) stderr INFO with format "{time:HH:mm:ss} | {level:<8} | {message}", (2) system log at DEBUG with daily rotation + 30d retention filtering on log_type=="system" (default), (3) trade log at INFO with daily rotation + 365d retention filtering on log_type=="trade", (4) error log at WARNING with daily rotation + 90d retention including exception info.

    **config.json**: Default configuration matching Settings._default_config().
    **.env.example**: Template with KIWOOM_ACCOUNT_PW= and KIWOOM_SIMULATION=true.
    **.gitignore**: Python defaults + .env, logs/, __pycache__/, *.pyc, venv/, .idea/, .vscode/.
    **requirements.txt**: PyQt5==5.15.10, loguru, python-dotenv, pytest, black, ruff.
    **__init__.py files**: Empty for all packages.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>
    - Settings class loads config.json and falls back to defaults
    - Constants (FID, SCREEN, LOGIN_ERROR) importable with correct values
    - setup_logging creates log directory and 3 log sinks
    - All tests in test_config.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test infrastructure (pytest config, conftest, test stubs)</name>
  <files>
    pytest.ini,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_config.py,
    tests/test_tr_queue.py,
    tests/test_session_manager.py,
    tests/test_event_handler.py,
    tests/test_real_data.py
  </files>
  <behavior>
    - conftest.py provides mock_kiwoom_api fixture that mocks QAxWidget dynamicCall
    - conftest.py provides mock_settings fixture returning Settings with defaults
    - test_config.py tests Settings loading (valid JSON, missing file, env vars)
    - test_tr_queue.py, test_session_manager.py, test_event_handler.py, test_real_data.py have test stubs (placeholder tests that pass, marking future test locations)
  </behavior>
  <action>
    **pytest.ini**: Configure with testpaths=tests, python_files=test_*.py, python_classes=Test*, python_functions=test_*. Add pythonpath=. so kiwoom_trader is importable.

    **tests/conftest.py**: Create shared fixtures:
    - `mock_kiwoom_api`: Uses unittest.mock to create a mock object mimicking KiwoomAPI interface (dynamicCall returns appropriate defaults, get_connect_state returns 1, connected/disconnected/tr_data_received/real_data_received as mock signals). Do NOT import from kiwoom_trader.api.kiwoom_api yet (it does not exist). Instead, create a MockKiwoomAPI class with mock pyqtSignals using unittest.mock.MagicMock for signal attributes.
    - `mock_settings`: Returns a Settings-like object with default config dict (no file I/O).
    - `tmp_config_file`: Creates a temporary config.json with known values, yields path, cleans up.

    **tests/test_config.py**: Tests for Settings class:
    - test_load_default_config: Settings with nonexistent path returns defaults with tr_interval_ms=4000
    - test_load_valid_config: Settings with tmp_config_file returns values from file
    - test_env_vars: With KIWOOM_SIMULATION=false in env, is_simulation returns False
    - test_constants_fid: FID.CURRENT_PRICE == 10, FID.VOLUME == 13
    - test_constants_login_error: LOGIN_ERROR.SUCCESS == 0
    - test_setup_logging: setup_logging() creates log directory without error

    **Stub test files** (test_tr_queue.py, test_session_manager.py, test_event_handler.py, test_real_data.py): Each file gets 1 placeholder test that passes (e.g., `def test_placeholder(): assert True`) with TODO comments indicating which behaviors will be tested in Plans 02 and 03. This satisfies Wave 0 requirements from VALIDATION.md.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - pytest.ini configured with pythonpath=.
    - conftest.py provides mock_kiwoom_api, mock_settings, tmp_config_file fixtures
    - test_config.py has 6 passing tests covering Settings, constants, and logging
    - 4 stub test files exist with passing placeholders
    - `python -m pytest tests/ -x -q` shows all tests green
  </done>
</task>

</tasks>

<verification>
- `python -c "from kiwoom_trader.config.settings import Settings; s = Settings(); print(s._config)"` prints default config dict
- `python -c "from kiwoom_trader.config.constants import FID, SCREEN, LOGIN_ERROR; print(FID.CURRENT_PRICE, LOGIN_ERROR.SUCCESS)"` prints "10 0"
- `python -c "from kiwoom_trader.utils.logger import setup_logging; setup_logging('/tmp/test_logs')"` creates /tmp/test_logs directory
- `python -m pytest tests/ -v --tb=short` shows all tests passing
</verification>

<success_criteria>
- Project structure matches RESEARCH.md layout (kiwoom_trader/ with config/, api/, utils/)
- Settings class loads JSON config with .env integration
- Constants module has all FID codes, SCREEN numbers, LOGIN_ERROR codes from RESEARCH.md
- Loguru configured with 3 daily-rotated sinks (system, trade, error)
- pytest runs with shared fixtures, all tests green
- Wave 0 test infrastructure complete per VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation/01-01-SUMMARY.md`
</output>
